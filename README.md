# 优选商城 - 电商展示网站

一个现代化的电商展示网站，使用 Vue.js 构建前端，Supabase 作为后端数据库服务，部署在 Netlify 上。

## 🛍️ 功能特性

- 📱 **响应式设计** - 适配桌面端和移动端
- 🏪 **产品展示** - 精选产品和分类浏览
- 🔍 **产品详情** - 详细的产品信息页面
- 🛒 **购物车功能** - 添加商品到购物车
- 📊 **分类筛选** - 按类别筛选产品
- 🎨 **现代UI** - 美观的用户界面设计

## 🚀 技术栈

### 前端
- **Vue.js 3** - 渐进式 JavaScript 框架
- **Vue Router** - 单页面应用路由
- **CSS3** - 现代样式设计
- **HTML5** - 语义化标记

### 后端服务
- **Supabase** - 开源的 Firebase 替代方案
  - PostgreSQL 数据库
  - 实时数据订阅
  - 用户认证
  - 文件存储

### 部署平台
- **Netlify** - 现代静态网站托管平台

## 📁 项目结构

```
ecommerce-website/
├── index.html              # 主 HTML 文件
├── app.js                  # Vue.js 应用主文件
├── styles.css              # 样式文件
├── package.json            # 项目依赖配置
├── netlify.toml            # Netlify 部署配置
├── supabase-config.js      # Supabase 配置文件
├── supabase-schema.sql     # 数据库结构文件
└── README.md              # 项目说明文档
```

## 🗄️ 数据库设计

### 4 张核心数据表：

1. **categories** (分类表)
   - 存储产品分类信息
   - 支持无限级分类扩展

2. **products** (产品表)
   - 产品基本信息
   - 价格、库存、分类关联
   - 特色产品标记

3. **cart_items** (购物车表)
   - 用户购物车数据
   - 数量管理
   - 用户隔离

4. **orders** (订单表)
   - 订单基本信息
   - 订单状态跟踪
   - 总金额计算

## 🛠️ 本地开发

### 环境要求
- Node.js 18+
- npm 或 yarn
- Git

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd ecommerce-website
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **配置 Supabase**
   - 在 [Supabase](https://supabase.com) 创建新项目
   - 复制项目 URL 和 API 密钥
   - 修改 `app.js` 和 `supabase-config.js` 中的配置
   - 在 Supabase 控制台执行 `supabase-schema.sql` 创建表结构

4. **启动本地开发服务器**
   ```bash
   npm run dev
   ```
   或使用 Netlify CLI:
   ```bash
   npx netlify dev
   ```

5. **访问应用**
   打开浏览器访问 `http://localhost:8888`

## 🌐 部署到 Netlify

### 自动部署（推荐）

1. **连接 GitHub 仓库**
   - 登录 [Netlify](https://netlify.com)
   - 点击 "New site from Git"
   - 选择 GitHub 仓库
   - 配置构建设置：
     - Build command: `echo 'No build step required'`
     - Publish directory: `.`

2. **环境变量配置**
   在 Netlify 控制台设置以下环境变量：
   ```
   VUE_APP_SUPABASE_URL=https://your-project-id.supabase.co
   VUE_APP_SUPABASE_ANON_KEY=your-anon-key-here
   ```

3. **部署完成**
   Netlify 会自动构建和部署你的网站

### 手动部署

1. **安装 Netlify CLI**
   ```bash
   npm install -g netlify-cli
   ```

2. **登录 Netlify**
   ```bash
   netlify login
   ```

3. **部署**
   ```bash
   netlify deploy --prod --dir=.
   ```

## 📋 功能页面

### 1. 首页 (`/`)
- 精选产品展示
- 促销横幅
- 导航到产品列表

### 2. 产品列表页 (`/products`)
- 分类筛选
- 产品网格展示
- 分页功能
- 搜索功能

### 3. 产品详情页 (`/product/:id`)
- 产品详细信息
- 库存显示
- 数量选择
- 添加到购物车

### 4. 购物车页面 (`/cart`)
- 购物车商品列表
- 数量修改
- 商品删除
- 总价计算
- 结算功能

## 🎨 UI 设计特点

- **现代化设计** - 采用渐变色和卡片式布局
- **响应式布局** - 适配各种屏幕尺寸
- **流畅动画** - 悬停效果和过渡动画
- **用户体验** - 直观的导航和操作流程

## 🔧 配置说明

### Supabase 配置

1. **创建项目**
   - 访问 [Supabase Dashboard](https://app.supabase.com)
   - 创建新项目
   - 记录项目 URL 和 API 密钥

2. **数据库设置**
   - 执行 `supabase-schema.sql` 文件
   - 配置行级安全策略（RLS）
   - 设置适当的权限

3. **环境变量**
   ```javascript
   const SUPABASE_URL = 'https://your-project-id.supabase.co';
   const SUPABASE_ANON_KEY = 'your-anon-key-here';
   ```

### Netlify 配置

项目已包含 `netlify.toml` 配置文件，包含：
- 路由重定向（支持 SPA）
- 安全头部设置
- 缓存策略
- 环境变量配置

## 🚀 性能优化

- **代码分割** - Vue.js 组件按需加载
- **静态资源缓存** - CSS/JS/图片长期缓存
- **图片优化** - 响应式图片显示
- **CDN 分发** - Netlify 全球 CDN

## 🔒 安全特性

- **行级安全（RLS）** - Supabase 数据库安全策略
- **HTTPS 强制** - 全站 HTTPS 访问
- **安全头部** - XSS、CSRF 防护
- **输入验证** - 前端数据验证

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 📞 支持

如有问题或建议，请：
- 创建 Issue
- 发送邮件至 [your-email@example.com]
- 访问项目文档 [项目文档链接]

---

**享受购物体验！** 🛍️✨